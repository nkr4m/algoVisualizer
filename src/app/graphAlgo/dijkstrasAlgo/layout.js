


export const metadata = {
  title: "Dijkstra's Algorithm",
  description: 'Finding the shortest path in a weighted graph.',
}

export default function RootLayout({ children }) {
  return (
    
        <>{children}</>

  )
}
