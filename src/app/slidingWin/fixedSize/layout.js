


export const metadata = {
  title: "Fixed Sized Sliding Window",
  description: 'Analyzing data within a fixed window size.',
}

export default function RootLayout({ children }) {
  return (
    
        <>{children}</>

  )
}
