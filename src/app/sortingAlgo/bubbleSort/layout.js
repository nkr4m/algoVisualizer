


export const metadata = {
  title: 'Bubble Sort',
  description: 'Swapping adjacent elements until sorted',
}

export default function RootLayout({ children }) {
  return (
    
        <>{children}</>

  )
}
