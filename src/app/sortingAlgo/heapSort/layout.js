


export const metadata = {
  title: 'Heap Sort',
  description: 'Using a heap data structure to extract elements in sorted order',
}

export default function RootLayout({ children }) {
  return (
    
        <>{children}</>

  )
}
