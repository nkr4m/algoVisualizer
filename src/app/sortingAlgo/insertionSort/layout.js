


export const metadata = {
  title: 'Insertion Sort',
  description: 'Building the sorted array by inserting elements in their proper place',
}

export default function RootLayout({ children }) {
  return (
    
        <>{children}</>

  )
}
